export type Item = {
    id: number;
    name: string;
    price: number;
    stock: number;
}

export type CartItemType = {
    itemID: number,
    quantity: number
    title: string,
    price: number,
    stock: number
}